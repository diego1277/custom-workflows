name: "setup-teste"
description: "setup teste"
inputs:
  terraform_version:
    required: false
    description: 'versao do terraform'
    default: '1.3.3'
   
runs:
  using: "composite"
  steps:
#  - name: setup git url.insteadof
#    run: |
#            cat "${{ github.action_path }}/templates/hello.yaml"
#            cp ${{ github.action_path }}/templates/hello.yaml ${GITHUB_WORKSPACE}/
  - id: polaris_custom_checks
    name: teste2
    run: |
            POLARIS_CUSTOM_CHECKS=$(mktemp)
            cat templates/hello.yaml | tee $POLARIS_CUSTOM_CHECKS
            echo "plaris_config=$(echo $POLARIS_CUSTOM_CHECKS)" >> $GITHUB_OUTPUT
    shell: bash
    working-directory: ${{ github.action_path }}
           
