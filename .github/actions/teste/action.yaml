name: "setup-teste"
description: "setup teste"
inputs:
  terraform_version:
    required: false
    description: 'versao do terraform'
    default: '1.3.3'
   
runs:
  using: "composite"
  steps:
  - uses: actions/checkout@v2
  - name: setup git url.insteadof
    run: |
            cat .templates/hello.yaml
    shell: bash
           
